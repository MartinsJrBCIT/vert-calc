#remember to delete all comments

import time

def maxreachCalc():
    
    
    
def minvertCalc():
    
    print("\nWe'll use a basketball rim to measure how much your vertical at least is.")
    print("Can you touch a basketball rim?")
    
    

def impMode():

    print("\n your standing reach first.")
    stdreachFt = input("\nFeet: ")
    stdrhFt = float(stdreachFt)
    
    stdreachIn = input("\nInches: ")
    stdrhIn = float(stdreachIn)
    
    stdReach = stdreachFt + "'" + stdreachIn
    stdRh = 12 * stdrhFt + stdrhIn
    
    print(f"standing reach: {stdReach}")
    
    time.sleep(1)
    
    #delete this later its for calculations
    print(stdRh)
    
    print(f"\nWith your awesome reach of {stdReach}, would your like to:")
    print("\n\n 1. Figure out your maximum vertical jump      2. Measure your minimum vertical with height milestones. ")
    
    vertCalc = float(input("\nType 1 or 2: "))
    
    while True:
        if vertCalc == 1:
            print("\nAll right, we will figure out your maximum vertical jump.")
            time.sleep(1)
            maxreachCalc()
            break
        elif vertCalc == 2:
            print("\nOkay then, lets figure out your minimum vertical with height milestones")
            time.sleep(1)
            minvertCalc()
            break
        else:
            
            print("Remember to type 1 or 2 only please.")
            vertCalc = float(input("\nType 1 or 2: "))
def metricMode():

    pass




#code here


print("\n Welcome to Martinothegoat's vertical jump calculator program. \n\n In this program you will be able to measure your vertical jump with just:\n\n 1. Your standing reach\n\n 2. The highest thing you can reach (Max jump touch)\n\n 3. Be capable of calculating your minimum max vertical based off of measure like if you can touch a basketball rim.")


print("\n\n First, would you like us to measure in the Imperial system(feet & inches) or the metric system (centimeters)?")


print("\n(Type 1 for Imperial, anything else for Metric)")

heightSys =float(input("\n Answer:"))

if heightSys == 1:
    
    print("\nNow using feet and inches for any calculation.")
    
    time.sleep(1)
    
    impMode()
else:
    
    print("\nNow using centimeters for any calculation")
    
    metricMode()
